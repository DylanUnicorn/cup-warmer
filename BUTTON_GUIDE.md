# 按键使用指南

## 按键定义

| 按键 | GPIO | 功能名称 | 说明 |
|------|------|---------|------|
| 按键1 | GPIO4 | 导航/选择 (NAV) | 在当前界面内切换选项或数值 |
| 按键2 | GPIO5 | 确认/进入 (CONFIRM) | 进入选中的菜单或保存设置 |
| 按键3 | GPIO9 | 返回/退出 (BACK) | 返回上一级界面或取消编辑 |

## 界面操作说明

### 主界面 (Main Screen)
- 显示当前温度、目标温度、加热状态

操作：
- **按键1**: 循环调整目标温度 (60°C → 65°C → 70°C → 75°C → 80°C → 85°C → 60°C...)
  - 适用于不同饮品需求（咖啡、茶、热奶等）
  - 调整后立即生效，屏幕实时更新目标温度
- **按键2**: 进入菜单
- **按键3**: 开启/关闭加热 (快速切换加热电源)
  - 按一次关闭加热
  - 再按一次恢复加热
  - 状态立即显示在界面上

### 菜单界面 (Menu Screen)
显示三个功能选项：
- 定时加热
- 预约加热
- 喝水提醒

操作：
- **按键1**: 向下滚动菜单项
- **按键2**: 进入选中的功能
- **按键3**: 返回主界面

### 定时加热界面 (Timer Mode)
设置加热时长（分钟）

可选时长：
- 15分钟
- 30分钟
- 60分钟
- 120分钟

操作：
- **按键1**: 切换时长选项
- **按键2**: 确认并启动定时加热
- **按键3**: 取消返回菜单

### 预约加热界面 (Schedule Mode)
设置预约加热时间（小时:分钟）

操作：
- **按键1**: 增加当前字段值
  - 小时：0-23循环
  - 分钟：0, 15, 30, 45循环
- **按键2**: 
  - 编辑小时时：切换到编辑分钟
  - 编辑分钟时：保存并返回菜单
- **按键3**: 
  - 编辑分钟时：返回编辑小时
  - 编辑小时时：取消返回菜单

### 喝水提醒界面 (Reminder Mode)
设置提醒间隔和开关

可选间隔：
- 30分钟
- 60分钟
- 90分钟
- 120分钟

操作：
- **按键1**: 切换开关状态(ON/OFF)和间隔时间
- **按键2**: 保存设置并返回菜单
- **按键3**: 取消返回菜单

## 界面导航路径

```
主界面(Main)
    │
    ├─[按键1]→ 调整目标温度 (60/65/70/75/80/85°C循环)
    │
    ├─[按键2]→ 菜单(Menu)
    │             │
    │             ├─[选择+按键2]→ 定时加热(Timer)
    │             │                  │
    │             │                  └─[按键3]→ 返回菜单
    │             │
    │             ├─[选择+按键2]→ 预约加热(Schedule)
    │             │                  │
    │             │                  └─[按键3]→ 返回菜单
    │             │
    │             ├─[选择+按键2]→ 喝水提醒(Reminder)
    │             │                  │
    │             │                  └─[按键3]→ 返回菜单
    │             │
    │             └─[按键3]→ 返回主界面
    │
    └─[按键3]→ 开启/关闭加热 (快速切换)
```

## UI更新特性

- **屏幕刷新周期**: 200ms
- **菜单滚动**: 循环滚动，无限循环
- **编辑状态指示**: 
  - 黄色高亮显示正在编辑的值
  - 底部显示操作提示
- **状态保持**: 设置保存后立即生效

## 注意事项

1. **配网期间**: 所有按键功能暂时禁用，等待WiFi配置完成
2. **屏幕栈深度**: 最多支持5级界面嵌套
3. **返回逻辑**: 按键3会按照进入顺序逆向返回
4. **温度控制**: 不受UI切换影响，始终保持恒温控制
5. **目标温度调节**: 在主界面按键1可快速调整6档温度（60/65/70/75/80/85°C）
6. **加热开关**: 在主界面按键3可快速启停加热，关闭后温控停止工作

## 功能亮点

### 🔥 主界面快捷操作
- **一键调温**: 无需进入设置，直接在主界面调整目标温度
  - 适合不同饮品：60°C温水、70°C绿茶、80°C咖啡、85°C红茶
  - 每次按键循环切换，界面实时显示新目标温度
  
- **一键开关**: 快速控制加热电源
  - 离开座位时关闭加热，节省能源
  - 返回时一键开启，继续保温
  - 开关状态在"加热状态"区域实时显示

## 实现细节

### 代码文件
- `components/lcd_display/include/lcd_display.h` - UI界面枚举和函数声明
- `components/lcd_display/lcd_display.cpp` - 显示实现（新增三个界面）
- `main/main.c` - 按键逻辑和状态管理

### 新增UI界面
1. `UI_SCREEN_TIMER` - 定时加热
2. `UI_SCREEN_SCHEDULE` - 预约加热
3. `UI_SCREEN_REMINDER` - 喝水提醒

### 状态变量
- `s_timer_minutes` - 定时时长
- `s_schedule_hour/minute` - 预约时间
- `s_reminder_interval/enabled` - 提醒设置
- `s_screen_stack[]` - 界面历史栈

## 待实现功能

以下功能当前仅有UI，实际任务逻辑待补充：

- [ ] 定时加热倒计时任务
- [ ] 预约加热触发任务
- [ ] 喝水提醒周期任务
- [ ] 蜂鸣器提示音
- [ ] 设置持久化保存（NVS）

## 测试建议

1. **菜单导航测试**: 循环按键1，检查菜单滚动是否正常
2. **界面跳转测试**: 测试所有界面之间的进入和返回
3. **编辑状态测试**: 在各设置界面测试数值修改是否正常
4. **屏幕栈测试**: 测试多级返回是否正确回到原界面
5. **配网干扰测试**: 确认配网期间按键不会误触发
